# Core runtime dependencies for inference + Optuna design-space exploration.
# Install torch first for your platform/CUDA (see https://pytorch.org/get-started/).
# CPU is sufficient for the surrogate; a GPU only speeds up large batched searches.
torch>=2.2
torch-geometric>=2.5
numpy>=1.24
scipy>=1.10
optuna>=3.6
matplotlib>=3.7
pandas>=2.0
PyYAML>=6.0
seaborn>=0.13   # optional: nicer S-matrix heatmap in the demo
